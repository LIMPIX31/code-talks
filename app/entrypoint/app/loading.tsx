import Box from '@mui/material/Box'

export default function Loading() {
	return (
		<Box position='fixed' width='100vw' height='100vw' sx={{ backgroundColor: 'background.default' }}>
			Loading
		</Box>
	)
}
